apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: checklist
    service: db
  namespace: checklist
spec:
  ports:
    - name: db
      port: 3306
      targetPort: 3306
  clusterIP: None
  selector:
    app: checklist
    service: db
